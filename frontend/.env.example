# Backend API URL — for local dev, unset to target localhost:8000 with direct CORS,
# or set to an empty string (VITE_API_URL=) to use the Vite proxy at /api.
# Set this to your production backend URL when deploying to Netlify.
# VITE_API_URL=https://your-backend.example.com
